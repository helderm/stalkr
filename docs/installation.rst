.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install stalkr

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv stalkr
    $ pip install stalkr
